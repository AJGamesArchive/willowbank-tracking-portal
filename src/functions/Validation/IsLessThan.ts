// Validation to function if a given number is less than another given number
export function isLessThan(baseNumber: number, minimumNumber: number): boolean {
  return (baseNumber < minimumNumber) ? true : false;
};